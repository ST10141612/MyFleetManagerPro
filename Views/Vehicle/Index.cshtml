@model IEnumerable<MyFleetManagerPro.ViewModels.AllVehiclesViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.vehicle.VehicleID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vehicle.Registration_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.driver.DriverName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fleet.FleetName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.vehicle.VehicleID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vehicle.Registration_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.driver.DriverName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fleet.FleetName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
